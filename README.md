# Solid Engine website

Build the static website

```sh
cargo r -- --watch
```

Launch development server

```sh
cargo r -- --watch
```

and serve the files in `docs` using e.g.

```sh
python3 -m http.server
```
